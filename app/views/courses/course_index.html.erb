<%= link_to "ADD Courses", new_department_path -%>
<ul>
  <% @courses.each do |d| %>
  <li>
    <span><%= d.name -%></span>
    <%= link_to "edit", edit_department_path(d) %>
    <%= link_to "delete", department_path(d), :method => :delete -%>
  </li>
  <%end%>
</ul>
