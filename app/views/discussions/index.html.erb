<div>

 <br>
 <br>
  <div id="dpt_header" class="clearfix">
    <h4>Discussions</h4>

    <%= link_to "+ Add discussions ", new_discussion_path(:course_session_id => @course_session), :class => "alr button" -%>

  </div>

  <ul id="departments">
    <% @discussions.each do |c| -%>
    <li class="clearfix">
	     <%= link_to c.title, discussion_path(c) -%></li>
	    <div class="edit-holder alr">
	      <ul class="edit-box">
	        <li>
             <%= link_to "Edit", "", :class => "edit-but" -%>
	          <ul class="edit-sub">
			        <li><%= link_to "unpublish", edit_assignment_path(c,:course_id => @course) -%></li>
		          <li> <%= link_to "Edit", edit_discussion_path(c,:course_id => @course) -%></li>
		          <li> <%= link_to "Delete", discussion_path(c, :course_id => @course ), :method => :delete -%></li>
	          </ul>
	        </li>
	      </ul>
	    </div>
    </li>
    <% end %>
  </ul>
</div>
